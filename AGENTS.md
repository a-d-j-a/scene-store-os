# Rules binding this agent to the user

## Hard rules (violations not tolerated)

- **Take the given time.** When the user gives time for a task, do not respond early. Completing early is a failure, not a win.
- **Never settle.** If you think you found something, try harder. First "found it" is not a finding; the deepest pit is one layer below. Verify from primary sources, not summaries.
- **No asking what was already answered.** Do not stop for clarification when the path is clear; continue with next steps instead.
- **No compromises on the project.** Full-fledged desktop. Nothing is stripped or scoped away to make it easier.
- **Create our own unless absolutely necessary.** Build every layer ourselves instead of reusing existing ones. Adopt someone else's code only when there is no way to build it ourselves (e.g. kernel boundaries, hardware drivers) — and justify it in writing each time.
- **Never hallucinate.** No invented facts, fabricated sources, fake findings, or empty promises. Everything claimed must be verified or explicitly marked as unverified; never state something as done when it isn't.
- **Use parallel sub-agents.** Delegate independent work to as many sub-agents as possible, in parallel, to boost efficiency. Do not duplicate their work while they run.
- **Use maximum amount of focus.** Apply the highest level of attention and rigor on the task at hand; no half-effort, no distractions, no drifting.

## Project: semantic scene engine (OS layer, not re-skin)

### Product goal (what we are building, never lose sight of it)
- An OS that is as fast and light as antiX/Puppy — boots and runs on hardware those run on — but does not look like 1999, and is deeply customizable.
- Modern appearance and deep customization must come from the OS layer itself (the semantic scene), not from piling on heavyweight shells/effects that cost the speed.
- Re-theme of running apps, OS-wide live theming, cross-app search/automation, replay — these are the customizability; a few MBs of memory and instant response are the speed. Both come from the same layer: the scene store.
- Any feature decision is tested against this goal: does it keep speed at antiX/Puppy level and does it make the look modern and the UI customizable?

- The OS owns the **meaning** of every native app's visible UI: regions, roles, labels, values, text, geometry, focus — per frame, versioned. Drawing is downstream of that.
- Novel asset is **understanding**, not rendering. Plan 9 devdraw, NeWS, BeOS = rasterizer channels. Fuchsia Flatland = retained composition, no semantics. On Linux the only semantic sources are accessibility trees (AT-SPI, GNOME's in-progress Newton prototype) — mirrors exported from apps for a11y consumers, not stored, versioned, or owned by the OS; no replay, no re-theme, no automation service. Windows UIA and macOS AX are read-mostly trees, same class. The OS-owned semantic scene remains open ground on Linux.
- Capabilities that fall out of the semantic scene: cross-app live search, cross-app UI automation as an OS service, deterministic whole-desktop replay/testing, re-theme of running apps, ghost-crash recovery, zero-cost capture.
- Honest boundary (stated, never hidden): browser/video/WebGL arrive as composited textures — effects applied, not semantically owned. Semantic scene is for native apps.
- Construction order: **scene store first** (versioned display lists + region semantics + op log). Compositor, effects, search, automation, a11y, rewind are consumers of it.
- Wire format is locked before any code.

## State

- Research/verification pass done (primary sources, not summaries): Plan 9 draw(3) live today; Fuchsia Flatland = retained composition only, VDC-hardware-delegated, no semantics; Windows UIA (Vista, 2007) and macOS AX trees = read-mostly a11y mirrors, no versioning/replay/store; Windows Timeline retired, Recall = screenshots, App Content Search = opt-in file index; Linux AT-SPI = pull-model D-Bus a11y mirror with chatty IPC, no screen coordinates for global scenes, opt-in toolkit support; GNOME Newton (Campbell, 2023-2024, STF-funded) = closest neighbor: push-based a11y trees through compositor, frame-synced, but explicitly pass-through only — "the compositor doesn't process accessibility tree updates; it only passes them through," no coordinates, no desktop enumeration, no store/log/replay, prototype only, unmerged, stillborn status as of 2026 (no upstream merge, a11y effort stalled); dogtail 1.0+/2.x = Wayland-capable AT-SPI automation with coordinate hacks (GTK4 shadow offsets), user-space tool, not an OS service. Conclusion: no shipped system owns a stored, versioned, replayable semantic scene; every existing semantic source is a derived mirror, not the primary artifact.
- Pass 3 (2026-08-03, source-level verification of current upstream code, direct reads not summaries): Chromium ax_tree.{cc,h} = current-state id→node map (id_map_, root_, data_); Unserialize applies one atomic AXTreeUpdate, fatal-error semantics, caches invalidated every update — no op log, no versioning, no replay, per-app in-process. AT-SPI atspi-accessible.c = client-side pull cache (cached_properties bitmask, ATSPI_MAX_CHILDREN partial caching, blocking D-Bus on miss). AT-SPI registryd/registry.c = OS-side "desktop frame" is hardcoded stubs: role 14 ("TODO: Get DESKTOP_FRAME from somewhere"), extents 1024x768, GetSize carries the literal TODO "Get the screen size", GetItems returns an empty array, GetAccessibleAtPoint returns a null ref, Contains always FALSE; registry's real state = app identity (SpiApplication), 30s liveness pings, event subscription lists. GTK gtkatspicache.c (main) = app-side point-in-time snapshot for AT prefill; ITEM_SIGNATURE "(so)(so)(so)iiassusau" (no geometry in it); "we take a snapshot here and return the items we have at the moment of the GetItems() call"; AddAccessible/RemoveAccessible signals unsequenced, no log/version/replay. AccessKit newton-prototype platform code (not just talks): update serialized as JSON, piped via fd to compositor; dropped entirely when no AT attached; no seq/ack/coordinates; UpdatesWanted re-sends full tree — pass-through confirmed in code. AOSP AccessibilityCache.java = per-service client cache of window/node infos, wholesale eviction on events, staleness guards. Flatland uber_struct.h = stateless UberStructLayer; epoch is a renderer structural-change hint, no semantics. wlroots wlr_scene.h = ROOT/TREE/SURFACE/RECT/BUFFER, enabled flag, parent-relative x/y, no roles/text. mutter mainline src/backends (168 entries scanned) = only meta-a11y-manager.{c,h}: keyboard/motion notify + modifier keysyms; zero files referencing atspi/accessib/atk. 2026-02 GTK hackfest (LWN 1058024): AccessKit GTK implementation stagnant, AT-SPI still the default backend; AT-SPI interfaces versioned on D-Bus but no mechanism to negotiate roles/events. Verdict: method kept, confirmed at code level — every existing semantic source is a derived unversioned mirror or a hardcoded stub; the stored/versioned/replayable scene is open ground, and no existing layer can host it without inheriting its exact limitation.
- Scene-store wire format spec: `scene-store-spec.md` (v0), locked before code. Pass 2 (2026-08) completed: server-driven-UI class (Phoenix LiveView etc.) proves the push-diff/owner-state pattern at scale and forced one spec hardening — committed scene = server truth, in-flight input = client truth (commit-vs-in-flight rule, section 7). Compositing scene graphs (Flatland, wlroots, gogpu) confirmed to exist → the novelty is not the scene graph itself; it is owned semantics + versioned store + op-log determinism + OS services on top. GTK 4.18 merged the AccessKit backend (2025); Newton remains unmerged — no semantic store anywhere. Method confirmed and kept after adversarial pass: no existing layer (web SSR/DOM, a11y trees, compositor scene graphs) can host the store without inheriting its exact limitation, and re-theme of running apps is impossible in every current DE. Nothing else was adopted; building our own remains justified per the create-our-own rule.
- Pass 3 closed 2026-08-03: the one previously blocked primary source was recovered — Newton a11y-v1.xml wire text read in full via Wayback Machine archive tar of the mwcampbell/wayland-protocols accessibility branch (capture 2024-03-13, commit a5fb7cd; post-squash dbe3e109 diff unread due to Anubis, but adapter code binds the identical interface set). Wire-level confirmation, strongest possible: "Tree updates and action requests use the schema defined by AccessKit... The current serialization format is the JSON serialization format implemented by AccessKit using serde. TODO: Finalize the serialization format."; wp_a11y_manager_v1 singleton, wp_a11y_surface_v1 per wl_surface (updates_wanted event creates a wp_a11y_updates_v1 receiver; action_request = fd), wp_a11y_updates_v1.send(fd) opaque blobs, double-buffered per-surface, "If more than one update is sent before a commit, all such updates but the last are ignored", "The client must send an initial update with a full accessibility tree to each new instance of this interface" (UpdatesWanted full-resend confirmed at wire level), and — decisive — "This allows servers to avoid retaining a full accessibility tree for each surface": the compositor is told to NOT retain the tree. No coordinates in the protocol (updates are opaque serialized fds), no seq, no ack, no versioning, no op log, no desktop enumeration; per-surface only. COSMIC (System76) checked: cosmic-a11y-unstable-v1.xml = magnifier + screen-filter toggles only, zero semantic content. AT-SPI registry.c correction: the literal "TODO - Get the screen size" is in impl_GetSize (extents 1024x768 hardcoded in impl_GetExtents). 2026 GTK hackfest (LWN 1058024): AccessKit GTK implementation stagnant, AT-SPI still default backend, D-Bus interface versioning exists but "no mechanism to negotiate the supported set of roles or events". GTK 4.18 merged AccessKit backend (May 2025, first Rust dep; Windows/macOS only; AT-SPI stays Linux default). Newton protocol never landed upstream: wayland-mirror staging listing + FreshPorts 2026Q3 list have no wp-a11y; it exists only in mwcampbell personal forks (wayland-rs/sctk/wayland-protocols accessibility branches). Verdict delivered to user 2026-08-03: keep the scene-store method; every existing semantic source is a derived unversioned mirror, an opaque passthrough, or a hardcoded stub; the stored/versioned/replayable scene remains open ground on Linux.
- Pass 4 (2026-08-03, implementation): scene store core COMPLETE and green — `make` clean with -Wall -Wextra, 16 black-box tests, 88 checks, 0 failures, deterministic across repeated runs (5/5). Done in this pass: (1) root-cause fix for the suite's SIGSEGV (exit 0xC0000005): `arena_alloc` realloc moved the arena base but `apply_ctx` held a copy of the pointer; `s->nodes` went stale on first arena growth (second CreateNode). Fixed with a `node **arena_back` back-pointer in apply_ctx, synced after every `arena_alloc`, set at all three ctx construction sites (live ingest, `replay_apply_log`, `macro_exec`). (2) Wire defect found by the tests and fixed at the spec first: checksum coverage was `[0, 12+record_length)` per spec line 25, which left the last 4 bytes of every payload unsigned — spec amended 2026-08-03 to `[0, 16+record_length)` with the 4 checksum bytes zeroed at compute time; engine `emit` + `scene_frame_check` + test `mk_frame` all aligned. (3) Engine additions: `scene_store_import_macro(dst, src, from_id, to_id)` — memory-level transfer of a recorded macro definition between sessions, required by the cross-app automation flow (record in one app, region-resolve-execute in another; wire format untouched). (4) Perf work measured by the new 10k-node bench: all four DFS sites (region_at, public search, search-reply emit, snapshot serialize, scene_store_walk) converted from per-call full-size malloc'd stacks to a fixed on-stack walk stack with heap fallback only for pathological depth. (5) Test-side fixes — all assertions now match engine/spec truth: TEXT_INDEX (0x800B, one record per SetText) must be drained before later outbound assertions; region tests use absolute session-space coords (spec §3: Rect is absolute, buttons must sit under the panel not the titlebar); ack consumed must cover the delivered scene_seq (11, not 9) else the flow-control gate stays shut; multi-store tests need per-store seq resets (engine requires strictly consecutive seq starting at 1 per session — a second store continuing the global counter dies with "non-monotonic seq"); ghost test re-creates the stale button so region resolution sees it. Environment fact: w64devkit (C:\Users\khalu\AppData\Local\Tools\w64devkit) has NO ASan/UBSan runtimes (`-lasan`/`-lubsan` cannot link), so `make test-dbg` is dead by environment; debug builds use plain `-g -O0` + gdb (worked well). Benchmark baselines (dev machine, 10,001-node scene, real wire path incl. framing+checksums+log): build ≈10k rec/s→~1.0M rec/s, text ingest ~18ms, outbound drain 0.1ms, region_at ~130µs/op, search ~530µs/op, snapshot 597KB serialized ~70MB/s, replay rebuild ~4.2M rec/s. Known accepted costs (documented, not defects): region_at and search are O(N) linear scans — fine for interactive rates at 10k nodes (sub-200µs), a spatial index / text index is a later optimization, tracked, not silently promised. Macros are per-session by design; cross-session replay goes through scene_store_import_macro.
- Pass 5 (2026-08-04, implementation): reference wire client layer COMPLETE and green — clean `make all` with -Wall -Wextra (no warnings), engine 16 tests/88 checks/0 failures, client suite 10 tests (9 wire + 1 threaded TCP)/262 checks/0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Delivered in this pass: (1) `scene_transport.c` — transport abstraction with two concrete transports: loopback FIFO pairs (single-threaded, growable) and blocking TCP (WinSock on w64devkit; POSIX path written to the same contract, kept but unexercised by this toolchain); threaded listener with one-at-a-time accept. (2) `scene_client.c` — seq-managed op stream exactly per spec v0: strictly consecutive seq starting at 1 per session; Ack is the 16B `consumed_input_seq+token` exception that does not advance the stream counter; WELCOME dispatch with retained-session continuation (same scene_id → `next_seq = sent_seq + 1`, ghost diff re-issue) vs fresh-session rebuild (reset to 1); reconnect op log holds scene-affecting ops only (payload minus seq); typed dispatch for all 11 server records (WELCOME/ERROR/SNAPSHOT/CAPTURE/SEARCH_RESULT/PONG/INPUT_POINTER/INPUT_ACTIVATE/INPUT_FOCUS/PRESENT_DONE/TEXT_INDEX) with strict length/checksum validation and `cli_violation` protocol closes; §6 `scene_snapshot_parse` with cap-guarded node/text/texture decode; pump reads at most ONE recv batch per call (blocking-TCP safe; callers poll to drain). (3) `scene_server.c` — the compositor seam: raw-byte feed, frame reassembly, `scene_frame_check` validation (magic/version/plen-vs-limits/checksum), engine ingest; ghost reconnect: detach ghost-marks the store, first non-ACK frame after re-attach rebases the stream via `scene_store_rejoin` (refusal = fatal SCENE_ERR_SEQ "ghost rejoin seq"); op-level failures emit the engine's own ERROR, frame-level ones via `scene_store_fail`. (4) Engine bug the wire tests exposed (reproduced first with a direct-engine repro, before touching the client path): `arena_alloc` free-list reuse path did not `(*count)++` while `op_destroy` decrements — after destroy→create the live-node count drifted down and every snapshot failed the `seen != count` guard (scene_store.c:536) with fatal "snap emit" (SCENE_ERR_LIMIT); fixed by making count = live nodes on the reuse path too; `snapshot_restore`/replay/macro ctxs share the fix via the same function. (5) All five walk sites converted to pre-order document order (snapshot_serialize, walk_arena, search-reply emit, scene_store_search, scene_store_walk); subtree destroy keeps post-order (children must die first), region_at left as-is (deepest-wins result provably identical). (6) Semantics confirmed and locked by tests: input flow control DROPS unacked presses (never queued — the ack only reopens the gate, a fresh injection is what arrives); a destroyed node's texts die with it (macro test snapshot texts_used = 2 = New + Save As, not 3 — "Open" belonged to the destroyed button); TEXT_INDEX must be drained before later outbound assertions. Wire format untouched by the whole pass.
- Pass 6 (2026-08-04, implementation): compositor core COMPLETE and green — the store's first consumer — clean `make all` with -Wall -Wextra (no warnings), engine 88 checks, client 262, compositor suite 8 tests/277 checks, 0 failures, deterministic (-O2 ×3, -O0 ×2). Delivered: (1) In-house 8×8 bitmap font (font agent failed twice with empty results; wrote it ourselves per create-our-own): `src/scene_font_data.c` (97-glyph table, ASCII 32..126 + box glyph at index 96, rows MSB-first top-down, glyph bits = bit 7 leftmost), `src/scene_font.c` (`scene_font_advance` INT_MAX-guarded, `scene_font_draw` opaque writes, clipped), header guard renamed `SCENE_FONT_H_INCLUDED` (collided with glyph-height macro). (2) `src/scene_compositor.c` (~800 lines): open-addressing render model keyed by node id (power-of-two caps, multiplicative hash, load 0.7); role-based default style table (server-owned dark look: window 0xFF202020, panel 0xFF2A2A2A, button 0xFF3C3C3C/0xFF555555 border, label transparent, IMAGE 0xFF1E1E1E, etc.); `resolve_style` = style ref ≥1 → server styles table, else role default, else GENERIC; text signature = FNV-1a over text_id/len/first-32-bytes (16 slots) → diff detects text changes; paint per node = fill → stroke → texture blit (per-node opacity) → text rows (last, so glyphs overwrite the stroke row); damage list: frame list reset each frame + pending list for compositor-side offers (texture re-register/release, style set) merged into the frame report, capped 32 with merge-into-last; first content frame clears the full fb (desktop background established once); `scene_compositor_input_pointer` forwards to the server seam; texture registry with per-ref `store_registered` flag (engine `scene_store_register_texture` rejects duplicate refs — compositor registers once per ref, re-register only refreshes pixels + dirties); `set_style` memcmp no-op + dirties referencing visible nodes → live re-theme of running apps. (3) Engine fixes found by the compositor tests (reproduced first in debug probes `dbg_move`/`dbg_ghost` in the temp dir): (a) node creates never set blend/opacity → default opacity was 0 → every node invisible; fixed at create AND ghost-resurrection (blend 0, opacity 255). (b) `scene_store_ghost_mark` iterated `1..node_count` but the arena is slot-based with gaps (a destroyed mid-list node frees its slot; live nodes then sit beyond the live count) → ghost re-create of a late node hit BAD_NODE and killed the session; fixed to scan `1..node_cap-1` skipping `id==UINT32_MAX` free slots (also caught my own first fix's OOB: `<= node_cap` would touch `nodes[node_cap]` past the allocation). (c) replay arena: `replay_build` from a checkpoint replays the ghost re-issued log (re-creates of nodes that exist in the checkpoint, non-stale) → BAD_NODE "replay build failed" on seek past a re-issue; apply_ctx gained a `replay` flag and op_create treats re-creates on the replay arena as resurrections (the live timeline had them as ghost resurrections — same semantics). (4) Read views (walk/vis/texts) now serve the ACTIVE view arena per the existing `req_arena` rule (replay arena in REPLAY mode, live otherwise) — snapshot/search already did this; `scene_store_node_count` stays the live count (locked by test_replay_determinism); new accessor `scene_store_view_seq` (replay cursor in replay, committed in live) is the compositor's diff gate — the compositor follows seeks deterministically (replay/seek service = first consumer proof: seek back/forward repaints exactly the differing rects, 2 rects at both seek(4) and seek(24)). (5) Test-side facts locked: a moved node overlapping a later sibling is correct z-order (later sibling wins); row 0 of a glyph sits on the button's stroke row so gap checks go on row 1; 'H' spans x+0..x+7, 'e' top rows empty; XRGB at node opacity 128 over fill blends per channel ((src*128+dst*127)/255) — re-registered blue 0x0000FF over 0x1E1E1E = 0xFF0E0E8E. Suite: 8 tests = empty/force, build paint (6 damage rects, exact pixels), move/text/visibility, re-theme (style table switch + live re-theme 1 rect, no-op identical set), texture (register/re-register/release + opacity blend), destroy + ghost reconnect (12-op re-issue at seq 13..24, zero diff repaint, rendered 24) + replay seek(4)/seek(24) + live-mode return, input flow (press → INPUT_POINTER+ACTIVATE stamped with scene seq; unacked press dropped; ack + fresh press delivered), node-vis ground truth (7 nodes, committed 11, texts intact). Wire format untouched by the whole pass.
- Pass 7 (2026-08-05, implementation): deterministic effects v1 + rounded-corner geometry + replay-safe anim suppression — clean `make all` with -Wall -Wextra (no warnings), engine 88 checks, client 262, compositor suite 14 tests/712 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Delivered: (1) **`scene_fb_chrome_round`** — single-pass per-pixel rounded-chrome rasterizer in `scene_fb.c/h`: interior pixels blend fill once, ring pixels blend border once (the earlier two-pass fill-over-border approach caused double-blend artifacts during fades). Radius 0 falls back to fill-over-stroke; bw=0 → fill_round; fill=0 → ring only. `rcorner_inset` formula corrected from the failed agent's `r - floor(r * sqrt(2*dy/r - (dy/r)^2))` to the canonical circle-algebra `r - floor(sqrt(2*r*dy - dy^2))`. (2) **`style_chrome` rewrite** in `scene_compositor.c`: radius>0 → `scene_fb_chrome_round`; radius 0 → fill-over-stroke. PANEL role default radius pinned at 4 (not 0); BUTTON=4, bw=1; WINDOW=8; SCROLLBAR=0; LABEL=2; IMAGE=0; PROGRESS=2; CHECKBOX=2, bw=1. (3) **Deterministic enter slide** — `anim_advance` restructured: ENTER free frame (t==TICKS) damages the full slide band `[base.y, base.y+h+ANIM_SLIDE]` (one rect that covers both the settled node and the tail rows it occupied while below its final spot during the slide); intermediate frames: base + sweep when off>0, base only when off==0. Slide constants confirmed: `ANIM_SLIDE=6`, `ANIM_TICKS=8`, off reaches 0 at t=7 (i=6), not t=8. enter ramp `{65,64,63,63,62,61,60,60}`. (4) **`scene_store_in_replay`** — new accessor in `scene_store.h/c` (`s->mode == SCENE_MODE_REPLAY && s->rnodes != NULL`) replacing the old seq-gap heuristic in `anim_replaying()`. The seq-gap heuristic (`committed_seq != view_seq`) failed when the replay cursor was at the head (view == committed → treated as live → exit anims fired during seeks); the mode-based accessor is correct for all cursor positions. (5) **`anim_clear_all` base-damage** — when replay begins or effects toggle off, `anim_clear_all` now damages each active anim's base rect before freeing, so stale phantom pixels (e.g. the exit-fade phantom left by a destroyed label) are cleaned up by the next frame's repaint. (6) **`anim_alloc` mid-enter band-damage** — when `map_sweep` replaces a mid-anim ENTER with an EXIT (node destroyed during its enter transition), `anim_alloc` damages the lost ENTER's slide band to prevent stale enter-fade tail pixels below the base rect. (7) **`anim_clear_all` and `anim_replaying` now live above `anim_advance`** — source order moved so `anim_replaying` (which calls the store accessor) and `anim_clear_all` (which calls `damage_rect`) are visible to `anim_advance` without forward declarations. (8) **Effects test suite expanded** from 2 to 7 tests (6 new, plus the existing build_paint test): `test_comp_effects_enter` (8-tick ramp, single-blend values, slide_y, alpha ramp, band damage at free frame with h=36), `test_comp_effects_exit` (7-tick phantom fade, border ramp), `test_comp_effects_identity` (8-tick settle → zero-damage, no effects after settle, enter→exit→identity cycle), `test_comp_effects_determinism` (two harnesses with identical op logs produce byte-identical framebuffers after settle, after enter, and after exit), `test_comp_rounded_corner` (panel r=4 corner pixel checks, SCROLLBAR r=0 control), `test_comp_effects_noanim_replay_ghost` (replay seek clears fade instantly, zero-diff ghost reconnect produces zero damage, mode-based detection works at all cursor positions including head). (9) **Node-vis ground-truth test** added (7 nodes, committed 11, texts intact). (10) **Compositor check count** grows from 277 to 712 (435 new checks across the7 new tests). Wire format untouched by the entire pass.
- Pass 8 (2026-08-05, implementation): automation consumer + search visibility filter + wlroots compositor skeleton — clean `make all` with -Wall -Wextra (no warnings), engine 88 checks, client 262, compositor 712, automation 10 tests/161 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Delivered: (1) **Automation test harness** (`tests/test_automation.c`, 10 tests): full loopback with `scene_compositor` (not raw server) — `harness_init` creates loopback transport, compositor (via `scene_compositor_new`), and client; `tickf` pumps 4 rounds of flush→recv→server_feed→outbound_drain→client_pump→compositor_frame; `build_app` creates 7-node UI (window, panel, Open button, Save button, label "Hello World", text input) through the client API. Tests cover: discover-by-text, click-discovered (compositor input_pointer → INPUT_ACTIVATE), type-into-field (set_text + search), macro record/replay (set_text mutations verified via search), cross-node workflow (search→click→set_text→search), visibility toggle (hide→search miss→show→search hit), move-and-verify (set_rect + search with rect assertions), multi-node search (3 buttons, alpha filter, exact match). (2) **Search VISIBLE filter** — `scene_store_search` and `emit_search_reply` both skip nodes where `!(nd->flags & SCENE_FLAG_VISIBLE)`: invisible nodes are no longer discoverable by text search (both engine-level and server-reply paths). The `emit_search_reply` inline loop (which duplicates the search walk for wire framing) was caught without the filter and patched. (3) **Wire client fixes exposed by automation tests**: `harness_init` was missing `scene_server_attach` call (every client op returned -1 because WELCOME never arrived — `welcomed` stayed 0 in `cli_emit` guard); `scene_node_id` is `uint32_t` not a struct (test had `.id.id` which was silently correct but wasteful). (4) **Custom wlroots compositor skeleton** (`src/iso_compositor.c`, `include/iso_compositor.h`): Linux-only (`wlr/backend.h`, `wlr/render/allocator.h`, `wlr/types.h`, `wlr/xwayland.h`, `wayland-server.h`), not compiled on Windows. Uses `scene_compositor_new/frame/fb` for rendering, `scene_loopback` + `scene_client` for shell node creation (desktop background + panel), `scene_server_feed` for client frame ingestion, `scene_compositor_input_pointer` for cursor/keyboard forwarding, output frame presentation via `wlr_output_commit`. Public API: `iso_server_new/free/start/scene_comp/server`, `iso_surface_new/destroy`. (5) **Test assertions corrected** for search semantics: substring match is by design ("Save" ⊂ "Saved!" — test changed to search for non-matching term "Savez" to prove text mutation); macro test rewritten to verify set_text mutations via search (SCENE_OP_FOCUS doesn't emit INPUT_ACTIVATE — only pointer clicks do). Wire format untouched by the entire pass.
- Pass 9 (2026-08-05, implementation): a11y consumer layer — clean `make all` with -Wall -Wextra (no warnings), engine 88 checks, client 262, compositor 712, automation 161, a11y 6 tests/50 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Delivered: (1) **`include/scene_a11y.h`** — public API: `scene_a11y_node` struct (id, parent, role, flags, rect, text_count, primary_text_id/len/data, child_count, depth); `scene_store_a11y_node` (per-node query), `scene_store_a11y_walk` (tree traversal with full a11y data per node, skips invisible+stale), `scene_store_a11y_focused` (current focus with full a11y data), `scene_store_a11y_focus_chain` (ordered visible+focusable node ids), `scene_store_a11y_find_by_role` (role-based search), `scene_store_a11y_children` (direct child enumeration). (2) **`src/scene_a11y.c`** — implementation built entirely on public store API (`scene_store_walk`, `scene_store_node_vis`, `scene_store_node_texts`, `scene_store_node_child_count`, `scene_store_focus`). No internal accessors needed. Walk callback filters invisible+stale nodes before forwarding to consumer. (3) **`scene_store_node_child_count`** — new accessor in `scene_store.h/c` (counts direct children by walking `first_child`→`next_sib` chain via idmap-resolved slot). (4) **`tests/test_a11y.c`** — 6 tests: node query (per-node a11y data, unknown/stale id handling), walk (count all nodes in 6-node tree), focus chain (3 focusable nodes in document order after focus op), role search (2 buttons, 1 label, 1 window, 0 menus), children (1 window child, 4 panel children, 0 leaf children, 0 unknown children), visibility filter (invisible node absent from walk/focus-chain/role-search/children). (5) **Wire protocol**: no changes — a11y is a read-only consumer; SET_STATE opcode deferred to when apps need to transmit a11y-specific state bits (checked/expanded/etc.) over the wire. Wire format untouched by the entire pass.
- Pass 10 (2026-08-06, implementation): rewind consumer (deterministic replay service) — clean `make all` with -Wall -Wextra (no warnings), engine 88 checks, client 262, compositor 712, automation 161, a11y 50, rewind 45 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Total: 1,318 checks across 6 suites. Delivered: (1) **`include/scene_rewind.h`** — public API: `scene_rewind` opaque type; lifecycle (`scene_rewind_new/free`); mode transitions (`scene_rewind_enter_replay/exit_replay`); navigation (`scene_rewind_seek/step_forward/step_backward/tell/head/tail`); diff (`scene_rewind_diff` + `scene_rewind_diff_free`) with `scene_rewind_diff_result` (created/destroyed/modified delta arrays). (2) **`src/scene_rewind.c`** — implementation: diff uses sorted-merge of a11y node snapshots between two seq points (`collect_arena_snaps` walks the active view arena via `scene_store_a11y_walk`, FNV-1a text hashing, `qsort` by id for merge). (3) **Store-side direct-mode APIs** — three new public functions in `scene_store.h/c` (`scene_store_begin_replay`, `scene_store_end_replay`, `scene_store_seek_to`) that bypass the wire op/seq pipeline, needed because the rewind layer is an internal consumer (not a wire client). These call `replay_build`/`replay_teardown` directly without going through `scene_store_ingest`. (4) **`tests/test_rewind.c`** — 6 tests: enter/exit replay, seek forward/backward, step forward/backward (with clamping), diff created/destroyed nodes, diff modified nodes (text change), determinism (two independent stores with identical ops produce identical diffs). Key bugs found and fixed: (a) `scene_rewind_diff` tail cases had created/destroyed arrays swapped — nodes only in `other` were filed as destroyed instead of created; (b) diff test expected 7 ops but `build_app` produces 8 (4 creates + 4 texts); (c) determinism test's `st()` calls shared a global `seq` counter across stores, causing the second store's text update to fail with "non-monotonic seq" (fixed with explicit `seq` reset per store). Wire format untouched by the entire pass.
- Pass 11 (2026-08-06, implementation): desktop shell layer — clean `make all` with -Wall -Wextra (no warnings), engine 88 checks, client 262, compositor 712, automation 161, a11y 50, rewind 45, shell 56 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Total: 1,374 checks across 7 suites. Delivered: (1) **`include/scene_shell.h`** — public API: `scene_shell` opaque type; `scene_shell_config` struct (bg/panel/button/menu colors, panel_height/radius/border, launcher apps); lifecycle (`scene_shell_new/free`); tree construction (`scene_shell_build`); per-frame update (`scene_shell_tick`); input handling (`scene_shell_handle_activate`); reconfiguration (`scene_shell_load_config`); output changes (`scene_shell_resize`). Config loader parses `Option=Value` text files. (2) **`src/scene_shell.c`** (~470 lines): config parser (Option=Value format, hex ARGB, comma-separated app list); build creates background(WINDOW), panel(PANEL), start_button(BUTTON), clock(LABEL), menu(MENU, hidden), pre-creates launcher item slots; tick reconciles task list via `scene_store_walk` with `walk_cb` filtering `id >= ID_BACKGROUND` to exclude shell-owned nodes, creates/destroys task buttons dynamically; handle_activate toggles start menu (VISIBLE flag), launches apps via `system()`, focuses windows via `scene_client_focus`; resize repositions all nodes; clock uses `time()` with `last_clock_min` debounce. (3) **`tests/test_shell.c`** — 10 tests: build correctness (5 nodes: background/panel/start/clock/menu), background rect (0,0,800,600), panel rect (0,560,800,40 with h=40), start click toggle (menu visible/invisible, items visible), task list (create window→tick→button appears→destroy→tick→button gone), clock update (HH:MM format, 5 chars), resize (1024x768 repositioning), config reload (scene_shell_config_load + scene_shell_load_config), non-shell click (returns 0), determinism (two shells with identical configs produce identical node trees). Key bugs fixed: (a) walk_cb matched background node as SCENE_ROLE_WINDOW → tracked as task → spurious button; fixed by filtering `id >= ID_BACKGROUND`; (b) determinism test never called tick → clock text empty; added explicit tick; (c) task destroy test needed flush BEFORE tick so store sees destroyed window; reordered. (4) **`shell.conf`** — default config file (dark theme: 0xFF1A1A2E bg, 0xFF16213E panel, 32px height, 3 launcher apps).
- Pass 12 (2026-08-06, implementation): shell visual polish — hover effects + 12h clock + config — clean `make all` with -Wall -Wextra (no warnings), engine 88, client 262, compositor 712, automation 161, a11y 50, rewind 45, shell 62 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Total: 1,380 checks across 7 suites. Delivered: (1) **Hover effects** — `scene_shell_handle_pointer(sh, x, y, buttons)` does absolute-coord hit testing over shell-owned nodes (start button, clock, menu items, task buttons) via `get_abs_rect` (walks parent chain accumulating relative offsets). Tracks `hovered_id`, applies/reverts `hover_style` (compositor style slot 1) on transitions. `scene_compositor_setup_hover_style(cp, fill, text)` allocates 2 style slots and configures slot 1 with hover colors. (2) **12h clock** — `clock_12h` config option; format `" 1:30p"` / `"12:00a"`, 4-6 chars. (3) **Config additions** — `hover_color` (default 0xFF2A2A4E), `clock_12h` (default 0). shell.conf updated with both. (4) **New tests** — `test_shell_hover` (pointer away → no hit, pointer over start button → hit, pointer off → no hit, style ref 1 applied), `test_shell_12h_clock` (12h format len 4-6). (5) **Key fix** — `scene_store_node_vis` returns parent-relative rects for child nodes; `get_abs_rect` walks the parent chain to resolve to absolute screen coords for hit testing.
- Pass 13 (2026-08-06, implementation): active task highlighting + task text refresh — clean `make all` with -Wall -Wextra (no warnings), engine 88, client 262, compositor 712, automation 161, a11y 50, rewind 45, shell 74 checks, 0 failures, deterministic across repeated runs (-O2 ×3, -O0 ×2). Total: 1,392 checks across 7 suites. Delivered: (1) **Active task highlighting** — `scene_compositor_setup_active_style(cp, fill, text)` allocates style slot 2; `scene_shell_set_active_style(sh, ref)` connects it to the shell. On each tick, `scene_store_focus()` is queried and the focused window's task button gets style 2; unfocusing reverts to style 0. `active_task_id` tracks the current button to avoid redundant set_style calls. (2) **Task text refresh** — on every tick, existing task buttons get their text refreshed from the window's current title (moved from the create-only path to the per-tick path, so title changes propagate). (3) **New tests** — `test_shell_active_highlight` (focus window → task btn gets style 2, unfocus background → style reverts), `test_shell_task_text_refresh` (window "v1" → button "v1", change to "v2" → tick → button "v2"). (4) **Focus op fix** — `scene_client_focus(h.cl, 0)` silently fails (node 0 doesn't exist); test uses `scene_client_focus(h.cl, 10000)` (background) for unfocus.
- Pass 14 (2026-08-10, implementation): multi-session compositor — clean `make all` with -Wall -Wextra (no warnings), 15 suites/1,738 checks green at commit time, deterministic. Delivered (commit `157f77e`): (1) **Layered sessions** — `scene_compositor_add_session(cp, sv)` attaches a foreign session's server as layer 1..n above the shell session (layer 0); each layer owns its store + render model + anims; a dead app layer (server death) freezes nothing — the next frame repaints its area as the desktop. (2) **Input routing** — pointer hit-tests app layers topmost-first via `scene_store_region_at`; the first session owning the point receives the event and becomes the keyboard focus (`focus_layer`); clicks on empty desktop fall to the shell; `scene_compositor_input_key` routes to the focused layer. (3) **`scene_compositor_remove_session`** repaints the removed layer's area (force full repaint) so the desktop is restored. (4) **`iso_drm.c` integration** — the guest compositor attaches the DRM session and shells (shell = layer 0). (5) **`tests/test_sessions.c`** (94 checks): session attach → window paints over the shell desktop, input to app layer, click-to-focus + key routing, app-death freeze, remove → desktop restored + input falls to shell, shell death is fatal to the compositor, fresh session reuse of the vacated layer. Wire format untouched.
- Pass 15 (2026-08-11, implementation): app launcher — the OS hosts real app processes as sessions. Clean `make all` with -Wall -Wextra (no warnings, incl. musl-gcc cross build), 15 suites/1,819 checks/0 failures, deterministic (launcher suite re-run 10× clean). Delivered (commit `a346eb3` + `095738b`): (1) **Non-blocking TCP listener APIs** in `scene_transport.h/c`: `scene_tcp_listener` / `scene_tcp_listen_new(port, *out_port)` / `scene_tcp_listen_accept` (NULL = none pending) / `scene_tcp_listen_destroy`; `scene_tcp_set_nonblock`; recv contract: 1 = would-block in non-blocking mode (blocking TCP recv never returns when drained — a child that blocks on its second recv hangs forever; launcher children must use non-blocking sockets + poll). (2) **`scene_app_new_on(t, target, cbs, ud)`** — scene_app over a TCP target (the child-side stack). (3) **`include/scene_launcher.h` + `src/scene_launcher.c`** — host service: spawn sets `SCENE_STORE_PORT`, non-blocking accept → `scene_server_new` + attach + `scene_compositor_add_session` (joined), per-session recv/feed/outbound-drain, reap dead sessions with slot compaction, `session_added`/`session_exited` callbacks (exited only for joined sessions), spawn timeout drops never-connecting apps (default 10s, configurable). (4) **`tests/test_launcher.c`** (81 checks, 6 tests: spawn/join/paint incl. titlebar band 0xFF1A1A1A + white title text built by scene_app's own TITLEBAR node; input routing logged by the child process; multi-spawn distinct layers; kill → reap → desktop restored; spawn timeout via "no-connect" child; shell session survives app death) + **`tests/test_launcher_app.c`** child binary (window 100,50,240,160, button 41000 "Go" at 120,182, auto-acks, event log, "no-connect" mode). Harness gives layer 0 a real shell client (CANVAS desktop node — the compositor only establishes the background after layer 0's first commit). (5) **Key fixes**: (a) the child's welcome-wait used `scene_client_next_seq > 1` which never advances before welcome (ops need welcome to emit) — added `scene_client_welcomed()` accessor and wait on it (root cause of the whole 11-failure wall, isolated with 6 probes incl. a scene_client child that DID receive WELCOME); (b) `scene_compositor_remove_session` now removes **by server identity, not layer index** — after the first reap compacts the layer array, a stale `s->layer` made the second removal fail silently, leaving the frozen window painted on screen forever (found via 2-child probe: count=0 but pixel stayed 0xFF202020); (c) `rn_text_capture` dropped an unused `cp` parameter; (d) `ws_ready` guarded behind `_WIN32` (unused-var warning under musl-gcc). (6) **Cycle 14 ISO verification** (`6c49c45`): scene-store builds zero-warning under musl-gcc on the codespace, ISO 25M, boots under QEMU (headless `-display none` + monitor `screendump`), iso-drm up at 1280x800@75 with scene nodes=5. Wire format untouched by the entire pass.
- Next: all seven core consumers proven + launcher layer ships real app processes into the compositor. For ISO path: link the launcher into iso_drm and spawn guest apps (the shell's launcher menu is the natural trigger), or wlroots compositor integration (skeleton written, needs Linux build env), or shell visual polish (task button styling, menu hover effects, system tray).
